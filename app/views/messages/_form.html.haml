= form_tag(messages_path, action: "create", method: "post") do
  = text_field_tag(:from_user, "From User...")
  %br
  = text_field_tag(:to_user, "To User...")
  %br
  = text_area_tag(:body, "Hi, nice site", size: "24x6")
  %br
  = submit_tag("Send New Message")